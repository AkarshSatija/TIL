`aws elasticbeanstalk restart-app-server --environment-name {{elb-env-Name}}`
- to restart elb serverApp from within elb shell

`eb deploy `
- deploy git repo to the elb

`eb ssh`
- Login into one of the instances of AWS
